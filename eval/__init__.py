"""Stock Swarm evaluation package.

`ground_truth` (realized forward returns) is GRADER-ONLY and must never be
imported from the swarm path — see the module docstring and
tests/test_ground_truth_boundary.py.
"""
