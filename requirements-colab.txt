# Colab dependency set for the stigmergic swarm.
#
# Install once per session:
#     pip install -r requirements-colab.txt
#
# vllm is the main heavy dep; it brings its own torch+CUDA wheel.
# The rest are for retrieval, embedding, and the renderer-audit external
# grounding lookups.

vllm
sentence-transformers
wikipedia
duckduckgo-search
requests
beautifulsoup4
tqdm
huggingface_hub
